{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "default",
  "description": "Base capability for the LunaBet remote shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
